{
  "name": "draw-pad",
  "version": "1.0.0",
  "private": true,
  "description": "Finger-drawing PWA with dinosaur stencils for young children",
  "scripts": {
    "pretest": "playwright install chromium || true",
    "test": "node tests/smoke.js",
    "start": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.40.0"
  }
}
